/// <reference types="react" />
export declare type ChannelType = 'inApp' | 'email' | 'webPush' | 'mobilePush';
interface Props {
    category: string;
    channels: Array<ChannelType>;
}
export default function CategoryPreferences({ category, channels }: Props): JSX.Element;
export {};
